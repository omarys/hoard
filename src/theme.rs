//! Dracula color palette — hoard's default colorscheme.
//!
//! See <https://draculatheme.com>. Colors are stored as `(r, g, b)` tuples so
//! they can be dropped straight into the config file.

/// Background — `#282a36`
pub const BACKGROUND: (u8, u8, u8) = (40, 42, 54);
/// Foreground — `#f8f8f2`
pub const FOREGROUND: (u8, u8, u8) = (248, 248, 242);
/// Comment — `#6272a4`
pub const COMMENT: (u8, u8, u8) = (98, 114, 164);
/// Cyan — `#8be9fd`
pub const CYAN: (u8, u8, u8) = (139, 233, 253);
/// Green — `#50fa7b`
pub const GREEN: (u8, u8, u8) = (80, 250, 123);
/// Pink — `#ff79c6`
pub const PINK: (u8, u8, u8) = (255, 121, 198);
/// Purple — `#bd93f9`
pub const PURPLE: (u8, u8, u8) = (189, 147, 249);
/// Red — `#ff5555`
pub const RED: (u8, u8, u8) = (255, 85, 85);
